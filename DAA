SLIP 9
Optimal Binary Search Tree & Sum of Subset (Backtracking)
DP
Backtracking
Q.1
Q.1 – Optimal Binary Search Tree

⧉
Copy
#include <stdio.h>
#include <limits.h>
#define MAXN 10

int optimalBST(int freq[], int n) {
    int cost[MAXN][MAXN] = {0};
    int i, j, r, length;
    for (i = 0; i < n; i++) cost[i][i] = freq[i];
    for (length = 2; length <= n; length++) {
        for (i = 0; i <= n-length; i++) {
            j = i + length - 1;
            cost[i][j] = INT_MAX;
